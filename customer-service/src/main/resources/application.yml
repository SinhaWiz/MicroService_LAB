server:
  port: 9001

spring:
  application:
    name: customer-service

  mongodb:
    uri: mongodb+srv://sinhartx2070_db_user:Password123@cluster0.fa7nmxd.mongodb.net/?appName=Cluster0
    database: customer_db

management:
  health:
    mongodb:
      enabled: false

eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://localhost:8761/eureka/
  instance:
    hostname: localhost
